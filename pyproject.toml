[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
]
ignore = [
    "B008",   # Do not perform function calls in argument defaults (FastAPI Depends / Query / Form)
    "B904",   # Within an except clause, raise exceptions with from err
    "B905",   # zip() without an explicit strict= parameter
    "E501",   # Line too long (formatador cuida da quebra visual)
    "UP042",  # Class inherits from both str and Enum (manter compatibilidade com SQLAlchemy Enum)
]

[tool.ruff.lint.isort]
known-first-party = ["app"]
